* https://docs.docker.com/desktop/setup/install/windows-install/

docker compose up -d	-> sobe o ambiente
docker compose down -> derruba o ambiente
docker compose stop -> para a execução
docker compose biuld -> recria a imagem do zero
docker compose ps -> lista o que está rodando

git status -> verifica o estado se algo mudou
git add . -> coloca as alterações no staging para liberar para salvar
git commit -m "msg" -> cria ponto de versão
git push - envia para o git
git pull - puxa atualizações que foram feitas em outro pc
