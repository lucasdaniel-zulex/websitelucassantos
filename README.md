* https://docs.docker.com/desktop/setup/install/windows-install/

<br>docker compose up -d	-> sobe o ambiente<br/>
<br>docker compose down -> derruba o ambiente<br/>
<br>docker compose stop -> para a execução<br/>
<br>docker compose biuld -> recria a imagem do zero<br/>
<br>docker compose ps -> lista o que está rodando<br/>

<br>git status -> verifica o estado se algo mudou<br/>
<br>git add . -> coloca as alterações no staging para liberar para salvar<br/>
<br>git commit -m "msg" -> cria ponto de versão<br/>
<br>git push - envia para o git<br/>
<br>git pull - puxa atualizações que foram feitas em outro pc<br/>
